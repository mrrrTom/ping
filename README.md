Terminal program (running on the arch).
Ping + trace functionality + may be in future - some net scanning and graph visualization.
Ping functionality includes small problems log, that should help to localize problem, when u are trying to ping smth and it does not work.

usage:
cd <build directory>
cmake
sudo ./ping
